> URL do webhook do n8n que recebe os leads do formulário e repassa o evento pro Meta Conversions
> API (deduplicacao com o Pixel via event_id). Nunca commitar o valor real em .env - apenas em
> .env.local (ignorado pelo git).
VITE_N8N_CAPI_WEBHOOK_URL=
